import React, { useState, useEffect } from 'react';

const Target = ({ data }) => {
  const [isOnline, setIsOnline] = useState(null);

  useEffect(() => {

  });

  return (<h1 style={{ fontSize: '6em' }}>QWEQE</h1>);
}

export { Target };